servers:
  playwright:
    type: local
    command: [npx, -y, "@playwright/mcp@latest"]
    enabled: true
    lifecycle: lazy

  memory:
    type: remote
    url: http://127.0.0.1:8080/mcp
    enabled: true
    lifecycle: lazy
